# 工具能力移植总结（2026-08-16，四任务完成）

## 概述

用户提供的 5 个工具源码（tools/sc/）+ 1 篇算法文章，按"高价值可自动化"
评估移植到本工具管线。四任务全部完成：逻辑移植 + 测试 + 真实验证 +
全量回归（2393 passed，零新增失败）。

## 任务 1：UnityCN 加密 bundle 解密提取 ✅

- **源码**：AssetStudio（C# 参考）+ UnityPy ArchiveStorageManager
  （Razmoth 同源 Python——直接集成，无需移植 C#）
- **产物**：`hanhua/core/unity/unitycn_decrypt.py`
  - `brute_force_key`：bundle + global-metadata.dat 双源 `\w{16}` 暴力
    探测（AES-ECB 解签名验证魔数）
  - `set_decrypt_key`：写入 UnityPy 全局（load 时自动解密数据块）
- **集成**：extractor 的 find_asset_files 收 unitycn_encrypted +
  extract_asset_file 自动探测 key；无 key 标记 blocked（报告可见）
- **测试**：4 passed（反向构造签名向量验证）
- **写回限制**：UnityPy encrypt 未实现——解密态写回（观察项）

## 任务 2：写回校验修复（UABEA patchcrc 等价）✅

- **源码**：UABEA/AssetsTools.NET（CRC 在编译 DLL——标准算法自实现）
- **产物**：`hanhua/core/unity/bundle_crc.py`
  - `unityfs_checksum`：hash=MD5(uncompressed)、crc=CRC32(IEEE)
  - `patch_checksums` / `verify_checksums`：header 定位 + 重算 + 写回
- **集成**：writer 写回验证处检测 UnityFS header 校验字段并记录
  （Unity 加载默认不校验——28 游戏实证无问题；严格路径可按需修复）
- **测试**：5 passed（合成 UnityFS 往返）

## 任务 3：IL2CPP metadata 字符串写回 ✅

- **源码**：il2cpp-stringliteral-patcher（Python 核心 80 行——直接移植）
  + MetaDataStringEditor（C# 参考）
- **产物**：`hanhua/core/unity/il2cpp_writeback.py`
  - 变长写回：译文追加文件末尾新数据区 → 重建 lookup_table
    （explicit 8 字节 / v39 implicit 4 字节）→ 更新 header 偏移
  - 无命中返回原样（防无意义重写）
- **验证**：6 passed + **真实 IL2CPP 游戏 11 个**（11446/18516/16512 条
  字面量提取 + 写回往返条数一致）
- **与现有 writer 的关系**：writer 已有原位写回（patch_metadata_strings
  + 完整验证链）——本模块为增强（任意变长），主路径不替换（防冲突）

## 任务 4：字体合并与字符集裁剪 ✅

- **源码**：Warcraft-Font-Merger（cmap/glyf 合并 + TrimGlyph 裁剪）
  + FilterRepeatCharacter（字符集去重——CSDN 文章被墙，核心按需求集
  自实现）
- **产物**：`hanhua/core/font/font_merge.py`
  - `collect_needed_chars`：译文+原文字符集去重（含 ASCII 开关）
  - `merge_fonts`：CFF（OTF）按需求集转 glyf（不超 TTF 65535 上限）
    + fallback 补缺（glyf+hmtx 同步）+ cmap 合并 + cmap 级裁剪
- **验证**：5 passed + 真实合并（SourceHanSans OTF 6.5 万字形 +
  DroidSans TTF fallback → 需求 12/12 覆盖，220KB）
- **关键修复链**（真实合并暴露）：CFF→glyf 上限 → cmap 先裁剪 →
  全局 glyphOrder 同步 → format14 uvDict 清理 → fallback hmtx 同步

## 依赖

- pycryptodome（UnityCN AES）
- fontTools（字体合并）
两者已装入 runtime（离线可用）。

## 全量回归

2393 passed / 28 skipped / 1 预存在失败（collect_known_names，与本次
改动无关）——四任务零冲突。

## 复查结论

- 提取管线：unitycn 走新分支（普通 bundle 路径不变）
- 写回管线：CRC 检测为新增报告字段（WriteResult.unityfs_checksum），
  不影响现有写回/验证
- IL2CPP：新模块独立（writer 原位路径不变）
- 字体：新模块独立（font 管线未接——缺字场景按需调用，接口完备）
