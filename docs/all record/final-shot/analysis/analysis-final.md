# final-shot 分析报告（闭环最终版）

## 1 游戏概况与语言分布

- 游戏：Final Shot（FPS 射击游戏，文本极少）
- 文本量：1301 识别条目（asset_unity 1035 + mono_csharp 186；库状态
  translated 320 · failed 0 · skipped 981）
- 语言分布（预检实证）：**英文/ASCII 1301**——纯英文游戏，无多语言
- 翻译池：320 条 actionable（其余 981 条为引擎系统串/结构串 skipped）

## 2 失败收敛过程

| 轮次 | 失败数 | 处置 | 确定性 |
|---|---|---|---|
| 首轮（08-12） | 2 | F24 调试日志模板串豁免（见 fix record）：`MEMORY: cur = {0}MB` / `CHANNELS: real = {0}` 是 Unity Debug.Log 格式串，变量名（cur/real/max）是脚本标识符无语义 → 译文保留变量名/整行回显都是合理行为 | 确定（恢复导入实证 320/320） |

## 3 失败分类（首轮 2 条 → 修复后 0）

| 形态 | 样本 | 真相 |
|---|---|---|
| 日志模板·译文含中文+变量保留 | `MEMORY: cur = {0}MB, max = {1}MB` → `内存：cur = {0} MB，max = {1} MB` | 译文合理（cur/max 变量名保留），旧判定 target_script_mismatch 误杀——F24 豁免后通过 |
| 日志模板·整行回显 | `CHANNELS: real = {0}, total = {1}` → 回显 | 调试日志串保留原文合理（与 ffs 769 条 low 系统串同类），旧判定 untranslated_text 误杀——F24 豁免后通过 |

## 4 正确性复核

- 恢复流程（写回成功后库清理）：scan_all 重建 → 导出解析（多行文本
  跨行存储修复：单行读取截断丢行 → 320 条误判失败）→ 新质量门验证
  导入 **320/320**（含 2 条日志模板修复后转通过）→ resume 写回
  320 条译文 PASS · failed 0
- 回归：tests/test_glossary_sanitize.py +10（F24 豁免 3 形态 +
  非模板形态不豁免 2 例）→ 全量 1656 passed

## 5 结论

**闭环成立**。首轮 2 条失败全部为调试日志模板串误杀（变量名保留/
回显都是正确行为），F24 豁免后 0 失败、320 条译文全部写回 PASS。
实机测试按用户指令跳过（2026-08-12 流程变更），云端审核停用。
