# final-shot 最终报告

## 概览

| 项 | 值 |
|---|---|
| 游戏 | Final Shot（FPS 射击游戏，文本极少） |
| 目录 | D:\游戏\final-shot |
| 闭环轮次 | run1（320 翻译 → 2 失败 → F24 修复 → 0 失败） |
| 翻译条目 | 1301 识别（translated 320 · failed 0 · skipped 981） |
| 语义审核 | **停用**（2026-08-12 流程变更：不用云端审核） |
| 写回 | 320 条译文 PASS（2 文本文件 + 字体部署）· revert 0 |
| 实机测试 | **按用户指令跳过**（2026-08-12 流程变更） |
| 汉化输出 | D:\游戏\final-shot_汉化（保留供人工复核） |

## 流程结果

1. **识别**：1301 条目（asset_unity 1035 + mono_csharp 186）；语言
   分布纯英文；翻译池 320 条 actionable
2. **翻译**（Hy-MT2-1.8B）：150s 完成 318 条，失败 2 条——全部为
   调试日志模板串（`MEMORY: cur = {0}MB` / `CHANNELS: real = {0}`）
3. **语义审核**：停用（用户指令）
4. **写回**（resume）：320 条译文 PASS · failed 0 · 字体部署 ✓
5. **实机测试**：跳过（见下章）

## 失败收敛（run1 2 → 0）

| 去向 | 条数 | 说明 |
|---|---|---|
| → translated（F24 豁免） | 2 | `内存：cur = {0} MB，max = {1} MB`（变量名保留合理）+ CHANNELS 回显（日志串保留原文合理） |

## 实机测试（按用户指令跳过，2026-08-12）

用户 2026-08-12 指令：「这个游戏做完，就先不进行实机测试了，然后
也不用云端审计了」——本游戏不执行《实机测试计划》7 章，不做云端
语义审核。写回质量门（占位符/回显/术语检查）作为替代机械验证全量
通过。

## 知识库沉淀

- **F24 调试日志模板串豁免**（final-shot 主产物）：形态 = 全大写
  标签 + 冒号 + 小写变量 + `= {n}` 占位符（Unity Debug.Log 格式串）
  → 译文保留变量名/整行回显都是合理行为；普通 UI 模板不豁免（防过宽
  测试实证）。与 ffs 769 条 low 系统串同属「日志文本保留原文」原则
- **恢复脚本 parse_export 多行修复**：导出多行文本跨行存储，字段
  状态机续行拼接（320/320 精确导入）

## 结论

**final-shot 闭环成立**：首轮 2 条失败全部为调试日志模板误杀，F24
系统性豁免后 0 失败、320 条译文全部写回 PASS。文本极少小游戏的
闭环流程（免实测/免审核）首次完整走通。观察项：无（skipped 981
条为引擎系统串，正确行为）。
