# backrooms 最终报告（闭环）

闭环跑：2026-08-11 00:33 ｜ 工具版本：backrooms 修复全链路生效

## 总体状态：✅ 干净闭环

| 模块 | 结果 | 说明 |
|---|---|---|
| 识别 | 0 bug | 946 条目，1 文本文件 + 2 v2 文件（272 资产 + 3676 il2cpp） |
| 翻译 | **211 成功 / 0 失败** | 首轮 209/4 → 修复 → 210/1 → 修复 → 211/0 |
| 写回 | 208 写入 | 重开验证 PASS，原子发布 |
| 质量门 | 拦截回显 + 目标脚本 + 术语错配 | untranslated_text / target_script_mismatch / glossary_mismatch |
| 知识库 | 沉淀 platform_name 形态 + 内置平台名引用 | itch 直译跨游戏根治 |
| 清理 | `_汉化` 目录 + 备份已删 | 做完一个删一个 |

## 核心战果：5 类失败根因全链路修复

初跑 4 失败（209/4）→ 逐条根因追查 → 修复 6 项通用机制 → 211/0。
详见 fix record/fix-11-backrooms.md：

1. **提取器漂移**：`nolog=`（boot.config 空值配置项）在 extractor 内联版缺
   kv_empty 分支 → 落 plain 被模型回显恒败（txt_format 已修，extractor 未对齐）
2. **数字邻接词豁免**：`4chan` 被拆出小写碎片 `chan` → 误判英文残留
3. **词级补译**：`itch page` 漏翻残留 → 短语单独翻译替换；裸翻译直译误译
   （痒页面）→ 逐词保留引用两跳确认（itch 页面）
4. **专名 references 重译**：`Markiplier was here` 纯回显 → 注入 (专名,专名)
   引用重译 → Markiplier 在这里
5. **保留型术语放宽**：learn 自动沉淀 FPS→FPS 后，质量门拒绝更忠实的
   「输入自定义帧率...」→ 保留型术语被翻译是合理行为
6. **内置平台名引用**：itch.io 的 itch 被模型当普通词直译「痒」→ 内置
   (itch, itch) 保留引用 + 知识库 platform_name 形态规则

## 性能

- 翻译：211 条全部成功，0 请求失败（服务复用 EXTERNAL 10500）
- 写回：1 文本文件 + 资产条目，重开验证 PASS

## 测试

- 全量 **1465 passed + 27 skipped，0 失败**
- 新增：数字邻接豁免（含反例）、词级补译两跳、专名重译、保留型术语放宽、
  extractor kv_empty 对齐

## 结论

backrooms 干净闭环（211/0）。本轮修复全部为通用机制（提取器对齐/词级
补译两跳/专名重译/保留型术语语义），无单游戏特判。itch 平台名直译是
1.8B 模型对低频小写专名的稳定误译——内置引用 + 知识库形态规则跨游戏
根治。进入下一游戏。
