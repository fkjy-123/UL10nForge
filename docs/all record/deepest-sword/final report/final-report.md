# deepest-sword 最终报告

> 2026-08-11 · **run2 闭环达成**（152 条译文 0 失败，写回 143 写入 + 9 合理保留）

## 概览

| 项 | run1 | run2 |
|---|---|---|
| 识别条目 | 152 | 152 |
| 翻译 | 149 成功 / 3 失败 | **152 成功 / 0 失败** |
| 写回 | WARN（140 写入） | WARN（143 写入 + 9 回显跳过） |
| 耗时 | 60.1s | 104.7s |
| 汉化输出 | — | 已删除（只留原版） |

## 修复链（run1 → run2）

| # | 修复 | 内容 |
|---|---|---|
| F4 | SAFE_KEEPERS 后缀边界 | `\b`（Unicode，中文=词字符）→ `(?![A-Za-z0-9])`——'Speedrun.com上的排行榜' 的 com 不再误当小写残留 |
| F5 | 版本模板单字母豁免 | `v{0}` 的 v 不再算独立小写词 → 版本模板回显放行 |
| F6 | 全大写词典词补译 | MAX（UI 词典）=最大，补译链可处理 → 0 失败收敛 |
| F7 | runner 清理范围 | 结束清理只删本游戏 slug（并行不误删工作区） |

## 写回说明（WARN 语义）

152 条译文：143 条写入 + **9 条回显跳过（译文==原文，非失败）**。9 条
逐条判定全部为合理保留：`v{0}` 版本模板、`MP: {0}/{1}`/`HP: {0}/{1}`
HUD 标签、`SFX` 缩写、`Discord`/`Twitter` 平台名、`SWORD` 艺术大写、
`ON UPDATE VAL`/`SCALED ENDING POSITION` 引擎串——译文==原文时写不写
结果相同，WARN 是准确反映，非缺陷。

## 译文抽检

- 占位符全保留：`MP: {0}/{1}`、`Playtime: {0}`、`HP: {0}/{1}` ✓
- 平台/专名保留：Discord、Twitter、Cosmic Adventure Squad ✓
- `ROTATE REPEAT`→`旋转/重复` ✓
- `MAX SEARCH OPTIMIZED`→`MAX 搜索优化版`（MAX 补译回显经模型确认
  保留，1.8B 能力边界下的可接受权衡）
- 调试串意译（`OBJECT NOT TWEEENING AT BEGINNING`→`该对象在初始化时
  不会执行任何操作。`）可接受

## 闭环结论

- ✅ 翻译失败：0
- ✅ 该翻而跳：0（跳过 494 条均为结构/类型引用，识别器结构性判定）
- ✅ 写回：143 写入 + 9 合理保留回显（WARN 语义准确）
- ✅ 汉化输出目录已删除

**deepest-sword 闭环。** F4/F5/F6/F7 修复进入工具版本，供后续游戏复用。
