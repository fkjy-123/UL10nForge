# interdream 地毯式排查分析（终版）

> 游戏目录：D:\游戏\interdream（DELTATRAVELER）· 2026-08-13
> 全本地推理（Hy-MT2-1.8B 翻译 / Qwen3.5-4B 审核）· 无任何云 API
> 前置文档：[[fix record/fix-record.md]]（F13 三修 + 34 条预审裁决 +
> 208 条写回缺陷译文登记终版）

## 1 运行概况

| 环节 | 数据 |
|---|---|
| 识别 | 18700 条 · 128 文件（Undertale 系对话：约 1400 条字面 `\n` 分隔 + 2881 条 `^` 计时码） |
| 翻译 | 3994 候选 · 3848 完成 · 146 失败（97.6%）· 记忆命中 0 · 耗时 8675s · 27 条/分 |
| 写回 | 3807 条 · 155 文件 · 闸门 WARN · 重开验证 True |
| 语义审核 | 571 送审（14.3%）· 228 不合格 · 11 术语沉淀 |
| 记忆 | 1826 条（库内） |

## 2 游戏结构专项：DELTATRAVELER 对话标记

Undertale 系对话脚本的三类标记是本游戏命脉，也是模型系统性丢失对象：

1. **行首 `* ` 对话符**——每行一个（`* You found the <color=#00FF00FF>[MOSS]</color>!`），
   模型常整段丢弃或移位（`* 只有无畏的人才能继续前进。` 移位成
   `只有无畏的人才能继续前进。* `）
2. **`^NN` 计时码**——形式多样：`,^05` 逗号后、`.^05` 句点后、`…^05`、
   行首 `^05* ` 接对话符、`?` 后、`)^05` 括号后
3. **字面 `\n`**——C# 转义（两字符）而非真换行分隔对话行（约 1400 条）

模型对这三类的行为：整段丢弃 `* ` 对话符（最常见）、丢计时码、字面
`\n` 行合并。F13 三修（见 fix record）后全部可拦截。

## 3 失败文本裁决（146 条终版）

预审 34 条分类：13 条词对误杀（F13a 修复；`WHAT'S YOUR NAME?` 纯误杀
完全放行，其余 12 条误杀消除后仍有真实对话符丢失拦截）+ 21 条真实
拦截。运行时新增 112 条，runner 完成后全量复验（文本快照，F13 修复
后代码）：

| 分类 | 数量 | 证据 |
|---|---|---|
| 判定误杀（F13 后放行） | 23 | 终验 `validate_translation_quality` 通过（f13-false-kill-final.txt） |
| 模型行为真实拦截 | 123 | 丢 `* ` 对话符 / 丢 `^NN` / 字面 `\n` 行合并 / 名单人名音译 / 调试串回显 |

确定性：同文本跨轮重跑一致 → 判定确定性成立（23 条误杀为 F13a 豁免
范围外的词对拦截，修复后放行）。

## 4 已写回缺陷译文（F13 修复前漏检，终版 208 条）

**208 条**（translation 列/写回最终值复验拦截）——分布：
`placeholder_mismatch` 166 · `newline+line_content` 27 · 三者混合 14 ·
`line_content` 1。全部为对话符 `* `/计时码 `^NN` 丢失、移位或字面 `\n`
行合并——F13 修复前写回，登记人工重译（与 incremental-rts F12 4 条
同处理）。根因：旧 `(?m)^\* ` 只匹配真行首，字面 `\n` 后的 `* ` 丢失/
移位全部漏检；`^NN` 裸形式丢失漏检。运行中快照 135 条 → 终版 208 条
（最后 378 条翻译的缺陷并入）。

## 5 词对误杀确认（13 条，F13a 修复）

对话脚本文本是叙事自由语义——(PLACE→地点) 杀「那个地方」、(NAME→名称)
杀「你叫什么名字？」、(Time→时间) 杀「时光/时机」、(Play→播放) 杀
「玩 UNO」、(SHOOT→射击) 杀感叹「哦，天哪」全部实证。F13a 判据
（`* ` 行首 + 括号/大写、`^NN` 计时码、全大写喊话 ≥3 词 + 句子标点）
识别对话脚本后豁免单 token 普通词词对；keep 型（target==source 保留
映射）与多词短语词对（FULL SCREEN→全屏显示）仍强制。

## 6 语义审核（终版）

- 送审 571 条（风险分流器，14.3% < 15% 硬约束）
- 不合格 228 条：语义一致性 77 · 术语一致性 55 · 信息完整性 50 ·
  否定 14 · 人物关系 10+ · 语气 5+3 · 其余 14
- 处置：review/review-report.md 逐条给建议；与 F13 缺陷重叠的对话标记
  问题并入 208 条人工重译登记；非 F13 语义问题人工确认后重译
- 术语沉淀 11 条 → 全局术语库（C5 门禁拒绝 0 条污染词对）
- 教训应用：审核报告引用运行中快照，裁决前对照写回最终值
  （incremental-rts 轮教训）

## 7 跳过文本判定（14706 条）

DELTATRAVELER 的 .dll 内含大量引擎串/代码串（Undertale 引擎运行时
文本），保守跳过。抽样确认：引擎日志模板/调试串/代码逻辑字符串占比
>99%，跳过正确（frm= 日志、FMOD/STUN/PrimeTween 引擎串、`====` 网格
警告模板均跳过）。

## 8 结论

- F13 三修是本游戏催生：对话脚本标记是 Undertale 系游戏命脉，
  旧代码漏检面大（字面 `\n` + 裸 `^NN` 全漏）——修复后复验拦截全部
  208 条写回缺陷
- 23 条判定误杀证明 F13a 词对豁免正确性；词对沉淀带语境保护落地
  （C5 门禁拒绝 0 条）
- 名单文本（credits）人名被 1.8B 音译——target_script_mismatch 正确
  拦截，但建议流程改进：名单类文本直接跳过（见 fix record 遗留 2）
- 208 条已写回缺陷译文登记人工重译（本游戏特判，不自动回写）
