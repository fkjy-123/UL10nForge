# baldis-fun-new-school-remastered 最终报告（闭环）

闭环跑：2026-08-11 01:53 ｜ 工具版本：baldis 修复全链路生效

## 总体状态：✅ 干净闭环

| 模块 | 结果 | 说明 |
|---|---|---|
| 识别 | 0 bug | 628 条目，1 文本文件 + 29 v2 文件（4321 扫描条目） |
| 翻译 | **628 成功 / 0 失败** | 637/15 → 640/6 → 627/1 → 628/0 |
| 写回 | 588 条写入 | 重开验证 PASS，原子发布 |
| 质量门 | 拦截回显/目标脚本/占位符/标签 | untranslated_text / target_script_mismatch / placeholder_mismatch / rich_text_mismatch |
| 知识库 | 纯小写回显引用两跳 + 换行合并兜底证据 | 跨游戏通用机制 |
| 清理 | `_汉化` 目录 + 备份已删 | 做完一个删一个 |

## 核心战果：12 项失败根因全链路修复（4 轮迭代）

初跑 15 失败 → 逐条根因追查 → 修复 6 项（词表/注释行/混合符号/UI 版本词/
属格碎片/引号专名）→ 6 失败 → 修复 6 项（专名联想/彩色标签/小写化专名/
换行合并/星号词表/交互动作词）→ 1 失败 → 修复 2 项（首译状态恢复/纯小写
回显引用两跳）→ 628/0。详见 fix record/fix-12-baldis.md：

1. **专名联想补词**：`Shirt Decal` → 模型补成 `T-shirt Decal` → 专名引用
   重译让模型按引用保留（T恤贴纸）
2. **彩色标签对整体丢失**：模型用引号替代 `<color>` 强调（稳定行为）→
   完整标签对放宽；单标签/{0} 仍失败
3. **星号词表/注释行/混合符号 token**：`*shit`、`//` 注释、`xChDC-Gs%OmaMl+g`
   → 结构跳过（识别层）
4. **换行合并兜底**：1.8B 稳定把多行合并单行 → 中文语义优先放行 +
   line_merged 证据标记；repair 失败后恢复首译状态（复查污染修复）
5. **纯小写回显引用两跳**：`outstanding citizen` 裸翻译回显 → 逐词引用
   → 杰出的公民（实测 3/3）
6. **小写化专名豁免**：Bossfight→bossfight 放行；功能词（the）仍拒绝

## 性能

- 翻译：628 条全部成功，0 请求失败（服务复用 EXTERNAL 10500）
- 写回：1 文本文件 + 资产条目，重开验证 PASS，原子发布

## 测试

- 全量 **1479 passed + 27 skipped，0 失败**
- 新增 11 个回归测试（含 4 条反例：the 残留仍失败 / 部分标签缺失仍失败 /
  单标签缺失仍失败 / 空段仍失败）

## 结论

baldis 干净闭环（628/0）。本轮修复全部为通用机制（识别形态规则/换行合并
兜底/引用两跳/首译状态恢复），无单游戏特判。模型能力边界：1.8B 对多行
合并、彩色强调引号替代、纯小写短句回显、专名联想补词是稳定行为——全部
以兜底+证据标记吸收。进入下一游戏。
