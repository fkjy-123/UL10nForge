# 地毯式排查升级计划（→ 0.25.0）

> 2026-08-10 启动。用户指令：对 D:\游戏 全部游戏实机跑完整流程
> （识别/翻译/写回），全环节记录到 `docs/all record/`，逐游戏闭环，
> 全部完成后跳 0.25.0、提交本地、更新远端、重写 README。
> 本轮是「重大更新」：以实机实测发现的问题驱动代码升级，
> 而非停留在代码层面推理。

## 0.4 知识库体系搭建（第一优先任务，2026-08-11）

> 用户指令：按 `C:\Users\mingming\Desktop\新建 文本文档.txt` 的六库方案
> 搭建知识库，**放在计划第一位，建完知识库再进行游戏闭环**。

### 0.4.1 目标（用户方案六库）

| # | 库 | 定位 | 解决 |
|---|---|---|---|
| 1 | Unity 结构库 | 地图系统 | 文本在哪/如何提取/如何写回/如何判断结构 |
| 2 | 汉化失败案例库 | 经验大脑 | 失败→根因→解决→影响范围→修复版本（FAIL 编号制） |
| 3 | 文本类型与处理规则库 | 该不该翻 | 漏翻/误翻/乱翻（UI/对话/技能/装备/成就/系统提示/调试/参数/代码 分类） |
| 4 | Unity 组件兼容库 | 后台成功游戏失败 | Text 乱码/TMP 方块/Dropdown 数据源/UI Toolkit |
| 5 | 翻译质量库 | 汉化品质 | 质量评分/常见错误/术语统一/上下文判断/AI 自检 |
| 6 | 写回与运行验证库 | 最终质量保证 | 写回记录/测试流程库 |

### 0.4.2 现状核查（2026-08-11）

- 只有 `~/.hanhua/knowledge.db`（knowledge_items 单表，domain 分库：
  text 25 条译例 / fail_case 88 条）+ glossary.db 用户术语表。
- 对照六库：失败案例库**部分**（结构不符：缺游戏/环境/根因/影响范围/
  修复版本字段）、文本规则库**部分**（无文本类型分类）、其余 4 库**没有**。

### 0.4.3 落地设计（扩展现有 knowledge.db，不另起炉灶）

domain 即库，kind 即子类，note 承载结构化明细（JSON）：

| 库 | domain | kind（子类） | 数据来源 |
|---|---|---|---|
| Unity 结构库 | `unity_structure` | unity_version / resource_type / text_location / detect_method / extract_plan / known_issue | docs 全景指南/资料大全 + 每游戏闭环登记（Unity 版本/形态） |
| 失败案例库 | `fail_case` | 提取/识别/分类/翻译/写回/显示/崩溃（升级 note 为结构化 JSON：game/env/issue/phenomenon/root_cause/solution/impact/fixed_version） | 现有 88 条迁移 + 每游戏闭环 record_case 完整字段 |
| 文本规则库 | `text` | 增强 kind：ui_text/dialogue/skill/equipment/achievement/system_msg/debug/param/code；action=translate/keep/skip；note 含优先级/要求 | 质量门规则 + 每游戏判定沉淀 |
| 组件兼容库 | `component_compat` | text / textmeshpro / dropdown / ui_toolkit / rewired / input_system… | font_support 等代码知识 + 实测 |
| 翻译质量库 | `quality` | scoring_case / term_consistency / common_error | 质量门评分 + 术语一致性（配合 glossary.db） |
| 写回验证库 | `writeback` | writeback_case / test_flow | docs/all record/writeback/ + 每游戏写回记录 |

### 0.4.4 实施步骤（第一优先，完成后才进入游戏闭环）

1. **schema 扩展**：knowledge.py 增加 6 个 domain 的 upsert/list 支持
   （表结构不变，domain/kind 分库即可；fail_case 的 note 升级为结构化）
2. **迁移**：现有 88 条 fail_case + 25 条 text 迁移到新 note 结构
   （保留 pattern/hits 不丢）
3. **种子数据**：从四份 docs 指南（Unity游戏文本位置与提取全景指南/
   Unity文本识别与提取资料大全/写回资料大全/写回调查报告/本地模型游戏
   翻译质量与游戏性保障指南）+ 识别形态清单 + 质量门规则提取种子入库
4. **查询接口**：kb 增加按 domain/kind 的查询与命中统计（现有
   format_reference_pairs 继续用于 text 域注入）
5. **闭环接入**：runner/record_case 登记完整字段（游戏/Unity 版本/形态/
   根因/影响范围/修复版本）；写回记录自动登记 writeback 域
6. **验证**：查询测试 + 种子完整性检查（6 库各 ≥ N 条、可查询）

### 0.4.5 完成定义

六库全部可查询（CLI 验证）→ 种子就位（Unity 结构库覆盖全部已识别
形态；失败案例库 88 条迁移完整）→ 游戏闭环自动沉淀生效 → 之后才
启动双游戏并行闭环（§0.5）。

## 0.5 运行模式：双游戏并行（用户明确指令，2026-08-11）

- **始终同时跑两个独立游戏**，互不干扰：各自独立 runner 进程、
  独立项目库（`~/.hanhua_sweep/projects/<slug>`）、独立日志、
  独立记录目录（`docs/all record/<游戏名>/`）。
- 流程完全一致：每个游戏都走 1-9 的闭环循环（识别/翻译/写回/记录/
  分析/修复/重测/闭环/清理），不做任何并行特有简化。
- 共享唯一 `llama-server`（10500 单服务）：两条 runner 的请求
  在服务端排队，质量无差异；同一时刻只启动一个服务实例。
- 每个游戏闭环后写三份文档（analysis/analysis-final.md、fix record/、
  final report/final-report.md），删除 `<游戏>_汉化` 只留原版。

## 0.6 当前进度（2026-08-11 记录，断点续作）

| 游戏 | 轮次 | 状态 | 剩余问题 |
|---|---|---|---|
| deadbeat | run1 89 失败 → … → **run7 0** | ✅ 已闭环 | 无（三件文档已写） |
| 222am | run4 66 条 0 失败 | ✅ 已闭环 | 待办 A：20 条音效/场景标签跳过（根因已定位，见下） |
| death-trips | **run1 0 失败** | ✅ 已闭环 | 无（20 条译文一次通过；130 条跳过逐条判定全部该跳） |
| deepest-sword | run1 3 失败 → **run2 0** | ✅ 已闭环 | 无（三件文档已写） |
| taxes | 历史闭环（08-10）→ 本轮重跑 run1 96 条 **0 失败** | ✅ 已闭环 | 无（写回 WARN = 1 条容量截断，已知行为） |
| a-catfiends | 历史 8 跑闭环（08-10，8 条失败判定合理接受）→ 本轮重跑 5 失败 → **F8-A/B 修复后 0** | ✅ 已闭环 | 无（F8-A 间隔动作词聚合重译 + F8-B self_heal 尾部缺口补全） |

**第二轮双游戏闭环达成（2026-08-11 18:00 前后）**：death-trips 20 条
0 失败一次通过 + deepest-sword 152 条 run1 3 失败 → F4/F5/F6 修复 →
run2 0 失败，双双写回（deepest-sword 143 写入 + 9 条合理保留回显）。

**第三轮双游戏（2026-08-11 18:30 前后）**：误选已闭环的 taxes/a-catfiends
重跑（§0.6 表格漏录 08-10 晚闭环的 6 游戏，见下）。复盘教训：**选游戏
必须排除已闭环（`docs/all record/<slug>/final report/` 存在即不重跑，
用户明确指令）**。重跑价值：a-catfiends 暴露 2 个遗留问题（F8-A/F8-B），
taxes 确认 0 失败稳定。
下一游戏对：**dollhouse + doog-hololive-fangame**（排除已闭环后按
D:\游戏 顺序）。

**已闭环游戏全清单（2026-08-11 18:30 盘点，选游戏对照此表）**：
captive、cell-machine、bird-builder、butterflies-episode-1、cosl、
containment-breach-hd-edition、count-my-coins-coin-counter、
crash-back-in-time、crusty-proto、deadbeat、222am、death-trips、
deepest-sword、a-game-about-literally-doing-your-taxes、
a-catfiends-impending-relapse、alisa-demo、backrooms、
baldis-fun-new-school-remastered、beware-the-shadowcatcher。
D:\游戏 杂目录（_test_/deadbeat_copy2/scan_tmp 等）不参与队列。

**本轮 F8 修复（a-catfiends 重跑暴露，见 fix record）**：

| # | 修复 | 说明 |
|---|---|---|
| F8-A | 间隔动作词聚合重译 | `_is_spaced_action` 剥 `{…}` 标签判定（{punch} 前缀曾致失效）+ 修复链 `_repair_spaced_action_translation`：`* Y A W N *` 聚合 `* YAWN *` 后模型能译出中文且标签原位保留（实验实证）——历史 6 条该形态失败判定「语义上应放行」但未实现，本轮实现翻译（用户指令：英文文本就要翻译） |
| F8-B | self_heal 尾部缺口补全 | 锚点限制 `missing >= dst` 过度保守：缺失是 src 尾部连续缺口（末尾占位符达原文末尾，如句末 `{w=3}{x}`）→ append 恢复原位置，可确定性补全；`</color>` 后仍有文本则拒绝（样式范围会拉长） |

**本轮 4 项修复（death-trips/deepest-sword 轮，见 deepest-sword
fix record）**：

| # | 修复 | 说明 |
|---|---|---|
| F4 | SAFE_KEEPERS 后缀边界 | `\b`（Unicode，中文=词字符）→ `(?![A-Za-z0-9])`——'Speedrun.com上的排行榜' 的 com 不再误当小写残留（译文完全正确被误拒） |
| F5 | 版本模板单字母豁免 | `v{0}` 的 v 不再算独立小写词 → 版本号模板回显放行 |
| F6 | 全大写词典词补译 | MAX（UI 词典）=最大，词级补译可处理 → 恒败收敛（SEARCH 触发大写动作判定阻断专名豁免后补译救回） |
| F7 | runner 清理范围 | 结束清理只删本游戏 slug 目录（原删整个 app_dir，双并行时误删并行 runner 工作区 → WinError 32） |

**死区接入完成（本轮）**：quality/component_compat 两库由「只种不用」
接入真实处理——质量门拒绝时检索 quality 域规则、写回失败时检索
component_compat 组件方案（runner 闭环四接入点 → 六库全部活跃）。

**待办 A 根因已定位（222am）**：`shower` 等 20 条音效/场景标签跳过 =
`DISPLAY_WORDS` 白名单盲区（165 词固定表；shower/city/bedroom/eggs/
ladder/mug 等常见场景词不在表内 → `is_key_style_identifier` 判为键
风格标识符跳过）。系统性修复方案（治理，不特判）：常见英语词典判定
或数据驱动扩充白名单，随后续游戏推进。

**本轮 4 项歌词链路修复（deadbeat run7 收官，见
`docs/all record/deadbeat/fix record/fix-record.md`）**：

| # | 修复 | 说明 |
|---|---|---|
| F0 | translate_lyrics 分块 | 1.8B 单次输出上限 ~700 字符（摘要式输出）→ _chunk_source 分块拼接 |
| F1 | max_tokens 缩放修正 | len//3+32（英语假设）→ len+128（中文 1 字符≈1.2 token） |
| F2 | llama-server ctx 6144 | 4096 装不下 prompt+完整输出（用户指示提高） |
| F3 | 歌词 newline 豁免 | 引擎单行存储歌词，模型分行输出是自然渲染，非结构破坏 |

**待办 A 根因已定位（222am）**：`shower` 等 20 条音效/场景标签跳过 =
`DISPLAY_WORDS` 白名单盲区（165 词固定表；shower/city/bedroom/eggs/
ladder/mug 等常见场景词不在表内 → `is_key_style_identifier` 判为键
风格标识符跳过）。系统性修复方案（治理，不特判）：常见英语词典判定
或数据驱动扩充白名单，随后续游戏推进。

**deadbeat run2 的 30 条失败根因（真实模型复现确认，2026-08-11）**：

1. `slash: 999` ×10（target_script_mismatch）：首译回显 → 词级补译裸翻
   "slash" → 模型输出「slash 翻译为 "斜线": 999」解释式垃圾 → 替换后
   **垃圾被判 passed**。待修：补译输出解释模式（「翻译为」）→ 引用重试/
   放弃；知识库种 slash→斩击 译例。
2. `miss: 999` ×8（untranslated_text）：词级补译两跳均回显 → 恒败。
   待修：知识库种 miss→未命中 译例（references 注入引导）。
3. `encore 1`（untranslated_text）：空格格式不在 label_values 分支
   （`^[a-z]{2,16}: ?\d+$` 需冒号）→ 词级补译 return None。知识库
   encore→安可 已种，run3 首译 references 注入验证。
4. `DeAD\nbEAt`（untranslated_text）：_artistic_case_echo 整条 len=9 > 8
   不豁免。待修：逐行检查（每行 ≤8 且艺术化大小写）。
5. 歌词长行 ×7-8（target_script_mismatch / 伪放行）：**重大 bug**——
   原文含单个日文汉字（「戦争」）被中文源放行条件
   （`_CJK.search and not 假名`）误判为中文源 → 整条英文原样放行
   （复现：1719 字符英文 passed）。待修：中文源判定收紧为
   CJK ≥ 2 且占字母 ≥ 50%。
6. 歌词分块尾部回显（「现代杀手」类 3183 字符，含假名 → 正常走分块）：
   最后一块含日文/俚语行模型回显英文 → 拼接后 target_script_mismatch。
   待修：multiline repair 行级回显豁免（echo_line_exempt，回显行=模型
   确认不可翻译的音效/外语行 → 记入 meta 放行，语义主体已译）。

**知识库状态**：text 域 25 条——encore→安可、miss→未命中、slash→斩击
（loaned_word 音游 HUD 术语）、hiss pop collection→Hiss Pop Collection
（proper_name, keep）均已种入（2026-08-11 补齐）；fail_case 域 88 条。
注：text 域有 1 条 (Guh) 歌词整段 multilingual_source 沉淀——是中文源
误放行 bug 的产物，代码修复后由 run3 学习闭环自然纠正。

**执行顺序（用户指令 2026-08-11）**：先完成 §0.4 知识库体系搭建
（第一优先），再启动双游戏并行闭环。游戏闭环前需落地的 6 项代码修复
已设计未实现（见上），知识库搭建与代码修复可并行推进，两者就绪后
启动 deadbeat run3 + 222am run4。

**知识库体系升级完成（2026-08-11，详见 `docs/知识库体系.md`）**：

- 六库 schema 扩展（knowledge_items 兼容升级，不重建）+ 88 条 fail_case
  结构化迁移 + 唯一重编号 + 25 条 text 迁移
- docs 种子 106 条入库（`scripts/kb_seed_docs.py`，幂等，source=seed）
- 查询接口齐备：`kb_query.py`（--stats/--game-stats/--keyword/--match/
  --solve/--all）+ KnowledgeBase.match_case/solve/library_stats/game_stats
- runner 闭环三接入点：unity_structure 注册（扫描后）/ record_case+
  match_case（失败时）/ writeback 注册（写回后）
- 修复 1（中文源收紧 CJK≥2 且占字母≥50%）已验证通过

**6 项代码修复状态（2026-08-11 全部实现）**：

| # | 修复 | 状态 |
|---|---|---|
| 1 | 中文源判定收紧（is_chinese_source） | ✅ 已验证 |
| 2 | _artistic_case_echo 逐行化（DeAD\nbEAt） | ✅ 已验证 |
| 3 | 词级补译解释模式拦截（slash 翻译为） | ✅ 实现（引用重试/放弃） |
| 4 | multiline 行级回显豁免（echo_line_exempt） | ✅ 实现 |
| 5 | _glossary_keep_echo（hiss pop collection） | ✅ 已验证 |
| 6 | encore 空格标签（_LABEL_VALUE_FORMAT 支持空格） | ✅ 已验证 |

slash→斩击 / miss→未命中 / encore→安可（loaned_word）与
hiss pop collection→Hiss Pop Collection（proper_name, keep）译例均已
在知识库 text 域，经 format_reference_pairs 并入 glossary 自动注入
（runner 已接入，无需额外代码）。两项闭环就绪，启动 deadbeat run3
+ 222am run4 双游戏并行。

## 0.7 写回逻辑层检查（用户指令 2026-08-11：写回游戏功能损坏，重新更改计划）

**用户反馈**：已闭环游戏写回后功能损坏——按键无响应（老问题）、游戏
卡住、逻辑性问题大量存在。文本翻译了但功能坏了。

**根因调查结论（2026-08-11，逐项实证）**：

1. **translated.txt 全量扫描**：19 个已闭环游戏写回记录中，代码标识符
   形态（camelCase/snake_case/大写常量/含数字）误翻**仅 1 条**
   （deadbeat `dEad→死亡`，游戏 stylization 大小写）——提取器形态防护
   有效，「逻辑字符串被当显示文本翻译」不是主渠道。
2. **写回验证是「字节自证」**：`_verify_saved_bundle` 重开比较的 expected
   来自补丁后的数据本身——只能证明「序列化往返无损」，发现不了补丁
   过程引入的结构破坏（rawstr 扩容插入使后续字段错位、字符串边界破坏）。
   这类破坏在游戏加载时表现为组件失败/按钮无反应/卡住，且不会让验证
   失败。
3. **rawstr 路径无容量约束**：`_patch_serialized_string` 对变长译文直接
   扩容插入字节，后续数据整体后移——正确性依赖「长度头+内容+对齐」的
   字段布局在插入后仍然自洽。
4. **「显示文本当逻辑键」**（知识库 writeback_case 已知：按钮文字/物品名
   被代码比较分发）——语义层问题，字节验证永远发现不了。

**本轮实现（F9，系统性通用规则，`hanhua/core/unity/logic_audit.py`）**：

| # | 检查 | 实现 | 行为 |
|---|---|---|---|
| F9-1 | 写回前敏感形态审计 | `audit_entries_before_writeback`：对 disposition=translate 的待写回条目做逻辑敏感形态检测（camelCase/snake_case/大写常量/含数字/类型描述符→warn；短词/纯小写长词/常见按钮文本→note） | 只报告不阻断（按钮文本 back/retry 大量命中短词，阻断误伤）；真实语料回测 warn 仅 dEad 类 |
| F9-2 | rawstr 扩容记录 | `audit_raw_expansion`：译文 UTF-8 字节 > 原文 → 逐条记录（扩容=插入字节，结构破坏源头） | 进审计报告，不阻断 |
| F9-3 | 重开逻辑验证 | `verify_logic_layer`（接入 `_verify_saved_bundle`）：译文字节存在性 + 字符串序列对齐比较（未翻译串保持原文、已翻译串变译文、扫描不可见的短译文豁免） | **失败整体拒绝写回**（副本保持原样，绝不落地坏产物） |

**产出**：`writeback/writeback.txt` 新增「逻辑层审计」段（warn 全列 /
note 统计抽样 / 扩容明细 / 重开验证失败详情）；runner 写回失败时
`_register_writeback` 登记 writeback_case（已接入）。

**下一游戏对（dollhouse + doog-hololive-fangame）闭环时启用**：写回后
检查 writeback.txt 逻辑层审计段——warn 条目逐条判定「真显示文本 vs
疑似逻辑字符串」（判定依据：对象类型/字段路径/游戏内上下文），疑似
逻辑字符串回退译文；扩容条目确认游戏加载正常。

## 0.8 写回逻辑层加强（用户指令 2026-08-11：审计要花大功夫，知识库案例转规则）

**用户反馈**：结构自证（F9）发现不了语义破坏——「文本翻译了但功能坏
了（按键没反应/卡住）」的真正机制是字符串被翻译后游戏代码按原名精确
匹配查找，文件结构完全合法。

**本轮实现（F11，`logic_audit.py`/`extractor.py`/`writer.py`）**：

| # | 检查 | 实现 | 行为 |
|---|---|---|---|
| F11-1 | 知识库案例转规则 | `knowledge.writeback_case_rules()`：5 条理论案例（NUL/占位符/编码/UnityEvent/逻辑键比较）→ 规则 ID + 实现定位；runner 写回段报告启用状态 | 案例不再只是备忘 |
| F11-2 | UnityEvent 对象信号 | `extractor._UNITYEVENT_SIGNALS`（m_PersistentCalls/m_Target/m_MethodName）→ 对象内字符串 structural 跳过（`unityevent_object`） | 识别层拦截事件绑定键（按钮无反应源头） |
| F11-3 | 统一逻辑键判定 | `logic_audit.logic_key_evidence(stripped, meta, obj_strings)`：对象角色 + 形态 → revert（类型描述符/事件绑定/代码对象比较词）或 report（标识符形态/比较词灰区） | 识别层与写回后**同一套规则** |
| F11-4 | 写回自动回退 | 补丁循环前对每条待写条目跑判定：revert → **不写补丁保留原文**（`note_logic_reverted`，独立计数不触发闸门）；report → 照写 + 审计段报告 | 高风险逻辑键不再进入汉化输出 |
| F11-5 | 同原文互斥 | `audit_repeat_consistency`：同对象同原文多处出现（doog 实证 Splash ×6）——组内结构跳过（键身份）或译文不一致 → 全组保留原文 | 防「译文+原文」混排断链 |
| F11-6 | 长度头自证 | `verify_string_length_headers` 接入重开验证：每译文出现处前 4 字节长度头 == 译文字节数 | 扩容后长度头未同步 → 拒绝 |
| F11-7 | 召回率监控 | runner 审计段：report 占比 > 5% → ⚠ 告警（识别层可能漏判，须人工复核） | 防哑信号 |

**产出**：writeback.txt 新增「知识库案例转规则」段（5 条启用清单）+
「逻辑键自动回退」段（revert 明细）+「疑似逻辑键 report」段 +「互斥
一致性」段；1572 测试通过（+16）。

**下一游戏对闭环时验证**：逻辑键自动回退真实生效（writeback.txt 回退
段非空或确认零命中）；report 段条目逐条判定（真显示文本 vs 需回退）；
互斥段确认无异常全组回退。

## 0.9 F12 语言选项直填 + 署名跳过（doog 17 条失败分类修复，2026-08-11）

**doog 17 条失败判定**（写回后全部低影响，F9/F10/F11 防护生效）：
11 条日语假名回显 = 1.8B 能力边界合理失败（保留日语原文安全）；2 条
`Language: ENGLISH` 乱译 = F12-A 修复；2 条 `林まか (pixiv: 10768714)`
署名 = F12-B 修复；1 条 `JAPONÉS` = 语言名回显惯例豁免（游戏语言列表
从不翻译语言名）；2 条 `The End`/`Doom transition` = 译文质量好被质量
门保守误杀（直调 passed=True，判定为保守边界观察不做修复）。

**本轮实现（F12）**：

| # | 检查 | 实现 | 行为 |
|---|---|---|---|
| F12-A | 语言选项词典直填 | `knowledge.language_option_translation`：`Language: ENGLISH` 标签形态 → 「语言：英语」确定性直填；`batch_translator._chat_each` 修复链最前接入（中文源放行后、模型调用前）；NFKD 去重音 + 原语名直填表（Español→espanol、日本語、русский…） | 封闭集合不走模型（doog 实证模型 4 次重试稳定乱译 → 恒败）；纯语言名保留原名惯例不受影响 |
| F12-B | 署名形态跳过 | `extractor._SIGNATURE_CREDIT_RE`（pixiv/twitter/facebook/instagram/artstation/deviantart/newgrounds/sketchfab/youtube/furaffinity/booth/fantia + ID/@用户名）→ `_structural_reason` 返回 `signature_credit` | 作者署名+作品 ID 是引用信息不是翻译内容，识别层跳过 |

**产出**：1578 测试通过（+6：语言选项直填 4 测 + 署名跳过 2 测）。

**下一游戏对闭环时验证**：语言选项条目不再进失败清单（或直填标记
`deterministic_fill` 可见）；署名形态无显示文本误杀（正文「Follow us
on twitter!」不受影响）。

## 0.10 F13 结构化格式 value 节点豁免软猜测降级（doog 分析期发现，2026-08-11）

**doog 跳过文本逐条判定发现 39 条哑信号**：xml `messages/message[N]/value`
位置（显示文本）被后置降级闸门误杀 33 条 + 合理跳 6 条（单字符/纯符号）：

| 误杀规则 | 样本 | 真相 |
|---|---|---|
| key_style 混合大小写 | FeeNGAh/PaNChee/RoCKTSu 等 hololive 罗马音歌词 | 真实显示文本 |
| `_QUALIFIED` 连字符标识符形态 | Konbanmio-n/Haro-bo | 罗马音台词 |
| 引擎串 PascalCase 形态 | FeeNGAh 等（`^[A-Z][a-z]+[A-Z]...`） | 同上 |
| credit_like 句子署名 | Get revived by Hololive's resident necromancer | 英文成就句 |
| log_template 冒号结尾 | Seleccione dificultad:（西语「选择难度」） | 西语 UI |

**修复（F13，`extractor.py`/`engine_strings.py`）**：

| # | 改动 | 实现 |
|---|---|---|
| F13-1 | 证据分层：xml value 节点 | `_should_downgrade_pending` 格式化分支：`textasset_format=xml` 且 inner_path 含 `/value` → 只做确定性硬结构降级（URL/GUID/JSON/数字/输入设备/绑定路径/base64/路径/已知引擎词表 `is_engine_string_core`）+ 无语言内容短串；软猜测反模式（key_style/PascalCase/credit_like/log_template/_QUALIFIED）全部豁免 |
| F13-2 | 引擎串拆核心判定 | `engine_strings.is_engine_string_core`（词表/前缀/哈希/绑定路径/富文本/表情名/语言名/HTTP 等形态标记明确分支）与 `is_engine_string` = core + 编程命名形态猜测（`_ENGINE_NAMING_PATTERNS` 驼峰/小写下划线/PascalCase）；raw scan 无结构上下文仍用全判定 |
| F13-3 | key 位置不豁免 | xml key 节点（PICKUP_BACKPACK 全大写键名）仍由 key_style 判定跳过——value 豁免不改变键名判定（键名翻译即断键） |

**边界验证**：doog 重跑——value 位置 936 pending（+33 恢复）/6 skipped（单字符纯符号合理）；key 位置 945 全跳过不变；`_should_downgrade_pending` 对 URL 机器数据仍降级。

**产出**：1580 测试通过（+2：value 豁免判定 + key 位置边界对照）。

**设计原则沉淀**：格式解析器（xml/json/yaml/csv）按**结构**判定可译性
（key/value 位置、_ID_LIKE 键名过滤）——其输出即显示文本证据；后置
降级闸门的「无结构上下文」软猜测规则（形态反模式）不得推翻格式判定，
只保留形态标记明确的机器数据降级。这与 typetree_display_field 的
credit 豁免（F11 前 lilys-day-off 实证）同源：**证据分层——结构
上下文 > 形态猜测**。

## 0. 核心原则（沿用治理框架）

- **哑信号治理**：跳过是哑的——被跳过的文本必须逐条审视「是否该翻」。
  遵循 `docs/识别形态覆盖与遗漏处理.md` 的 SOP：定位载体形态 →
  证据分层审查 → 真实样本锚点 → 形态登记 → 回归。
- **证据分层**：确定性证据 > 形态性证据 > 猜测性证据；硬结构规则
  任何证据下生效，软猜测规则不得推翻确定证据。
- **闭环才算完**：每个游戏的问题全部修复后，用**升级后的工具版本**
  重跑该游戏全流程直到无问题——绝不用旧版本做下一个游戏。
- **每条记录都有价值**：成功文本查质量与应译性；失败文本查根因；
  跳过文本查是否该翻；写回查成败详情。

## 1. 范围

- 目录：`D:\游戏`（129 项，含测试残留/汉化输出等非目标项，按 `_survey.json`
  的 unity 清单过滤，目标 ~120 个真实游戏）。
- 每个游戏三模块全流程：`scan_all`（识别）→ 翻译（本地 Hy-MT2 真实调用）
  → 写回（真实译文 write_all）→ 记录 → 分析 → 修复 → 重测闭环。
- 汉化输出目录（`<游戏>_汉化`）验证完成后删除，只保留原版。

## 2. 基础设施（先建，一次投入复用全程）

### 2.1 记录结构 `docs/all record/<游戏名>/`

```
summary.md        # 排查总结：统计/发现的问题/修复项/闭环状态
text/
  translated.txt  # 成功文本：来源/键位/原文/译文/置信度/原因/质量评分
  failed.txt      # 失败文本：来源/键位/原文/译文/失败原因/详情
  skipped.txt     # 跳过文本：来源/键位/原文/跳过原因/判定结论（该翻/不该翻）
writeback/
  writeback.txt   # 写回逐文件记录：文件/成功失败/详情/验证结果
```

格式参照 `docs/fail record/`（来源/键位/原文/译文/原因 五字段）扩展。

### 2.2 runner：`scripts/all_record_runner.py`

单游戏完整流程执行器（复用 GUI 同一条代码路径：Project + BatchTranslator +
LocalModelManager）：

1. 清空项目库（幂等全新建库）→ `scan_all()` → 记录识别统计
   （形态统计/未知形态警告/status/confidence/阻断项）
2. 启动本地模型 → 翻译全部 actionable 条目（真实模型调用）
   → 记录 stats（成功/失败/重试）
3. 导出三类文本记录（translated/failed/skipped，全字段）
4. `write_all()`（真实译文写回）→ 记录逐文件写回详情
5. 汇总 summary.md

### 2.3 速度策略（用户授权：加快但保质量）

- 本地 `llama-server` 单请求串行 → 并发提升无吞吐收益，**速度旋钮是
  `local_batch_size`**（默认 8 → 提到 16 起步）。
- 质量底线：batch 过大长上下文易丢字/回显。每游戏观察 failed 率，
  batch 16 失败率异常上升则回退。
- 模型配置沿用用户设置（settings.json）。

## 3. 单游戏执行流程（闭环循环）

```
1 扫描     scan_all → 记录形态统计/警告/异常
2 翻译     真实翻译全部 actionable → 记录三类文本
3 分析-成功 抽检译文质量与匹配：译文差→翻译质量修复；无关文本→判定跳过（记入 skipped）
4 分析-失败 逐条找根因：系统性彻查（同类问题全解），修复代码+测试+锚点
5 分析-跳过 逐条判定：该翻而跳→识别功能修复（形态/证据分层/先验），防同类遗漏
6 写回     write_all → 逐文件记录；失败→精确定位→根源修复
7 修复回归 修复后跑相关测试 + 用升级版本重跑本游戏全流程
8 闭环判定 无失败/无该翻而跳/写回全绿 → 闭环；否则回到 3-7
9 清理     删除 <游戏>_汉化 输出目录（保留原版），进入下一游戏
```

**版本纪律**：每次修复即递增补丁版本提交（本地）；下一游戏必须用
最新版本跑（修完先提交再继续）。

## 4. 问题分类处理表（分析时的判定标准）

| 类别 | 观察 | 动作 |
|---|---|---|
| 成功-译文质量差 | 译文不通/错译/丢占位符 | 翻译侧修复（prompt/glossary/保护占位符） |
| 成功-无关文本 | 键名/路径/枚举被译 | 识别侧加硬结构规则或证据分层（该文本不该进池） |
| 失败-untranslated | 模型回显 | 该文本该不该翻？该翻→提取侧剔除噪音/结构；不该翻→提取侧跳过 |
| 失败-其他 | 超时/格式错 | 按原因分类，系统性修复 |
| 跳过-该翻 | 对话/UI 未进池 | 识别侧修复：形态先验/证据分层/提取器覆盖；登记形态锚点 |
| 跳过-不该翻 | 键名/引擎串/结构 | 无需动作（记录判定） |
| 写回-失败 | 某文件写回错 | 定位 writer 代码路径，根源修复 + 回归测试 |

每项修复遵循：**根因定位 → 代码修复 → 单测/锚点 → 全量回归 → 该游戏重测闭环**。

## 5. 完成与交付

- 全部游戏闭环后：版本跳 **0.25.0**（重大更新）。
- 本地提交全部变更；**更新远端**（用户本次明确要求 push）。
- **重写 README.md**（反映识别治理框架、地毯式实测成果、使用方式）。
- 清理：`docs/fail record/` 旧散档可归档；临时探针/残留目录清理。

## 6. 风险与约束

- **时长**：~120 游戏 × 每游戏 30-90 分钟（翻译为主）——双游戏并行
  推进（见 0.5），每对游戏闭环后再进下一对，进度记录在 summary 与本
  文档 0.6。
- **模型质量波动**：失败率高时优先查 batch 设置，其次查文本噪音源头。
- **硬盘**：每游戏闭环即删汉化输出，只留原版。
- **用户中断**：任何一步用户叫停即停；已闭环游戏记录保留，断点续作。
