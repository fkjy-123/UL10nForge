# 四模型冒烟记录（2026-08-13，任务一阶段 0 验收）

> 执行：`python scripts/model_smoke.py`（全量：translate/review/rerank/embed
> + 104 条审核成本基线）。全本地 llama.cpp，零云端调用。

## 硬件分配方案（真实探测）

本机 12GB GPU（可用 8.86GB）+ 32GB RAM → **≥8GB 档双 GPU 常驻**：

| 模型 | 后端 | ctx | keep_alive | 说明 |
|---|---|---|---|---|
| translate Hy-MT2-1.8B | gpu | 8192 | 常驻 | 显存预估 ≤ 8.86×0.9 |
| review Qwen3.5-4B | gpu | 8192 | 常驻 | 显存预估 ≤ 8.86×0.9 |
| rerank Qwen3-Reranker-0.6B | cpu | 4096 | 常驻 | 固定 CPU |
| embed Qwen3-Embedding-0.6B | cpu | 4096 | 常驻 | 固定 CPU |

## 单模型冒烟

- **[translate] PRESS TO START → '按"PRESS"开始'（0.9s）**——Hy-MT2 输出
  含引号装饰，质量门将复查（已知模型行为，排查流程已处理）
- **[review] 四级判定**（`--reasoning off` 生效，2.2s/条）：
  - `PRESS TO START → 按媒体开始` → **MAJOR**（"媒体"歧义，语义错译检出 ✓）
  - `PRESS TO START → 按开始键开始` → MINOR（语义正确、冗余）
  - `Save the game → 保存游戏` → PASS
  - `I don't have enough mana. → 我的法力不足。` → PASS
- **[rerank] Resume 样本排序**：**已诊断（2026-08-13 复核）**——rerank
  实际工作正常，排序有语义（index=2「回到主菜单」得分最高）；score 显示
  0.000 是因为 llama.cpp 输出 **raw logits**（e-06~e-07 量级），冒烟脚本
  `{:.3f}` 格式化后全为 0.000。修复：冒烟脚本改为 sigmoid 归一化打印
  （`_sigmoid`，排序不变）。**后续 rerank_gate 实现必须用 sigmoid 归一化
  后分数做阈值，不能直接比较 raw logits**
- **[embed] 1 条向量**：**dim=1024**（非预期 512，Q8_0 模型实际维度），
  输出有效浮点（0.9s）。**阶段 4 Embedding 向量检索实现按 1024 维设计**

## 审核成本基线（104 条短句，真实送审）

| 指标 | 值 |
|---|---|
| 总耗时 | 166.4s |
| 平均 | **1.60s/条**（关闭思考后；开启思考时 3.84s/条） |
| 吞吐 | 0.62 条/秒 |
| 失败 | 0 条（104/104 成功） |
| 级别分布 | PASS 103 / MAJOR 1 |

**分流阈值参考**：15% 调用率预算下，2000 条译文 ≈ 300 条送审 ≈ 8 分钟；
10000 条 ≈ 40 分钟（可接受，排查流程按此规划）。

## 结论

- 阶段 0 验收点全部达成：四模型真实可调用、硬件分配正确、
  0.6B×2 恒 CPU、审核 JSON 稳定输出（--reasoning off 固化在
  review spec server_args）
- 观察项（待任务三）：rerank score 恒 0 需诊断；embed 维度 1024
  需按实际维度调整检索实现
